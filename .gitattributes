# Normalize text files and treat images as binary
* text=auto eol=lf

# Treat common binary image formats as binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# SVGs are text (so they can be diffed)
*.svg text

# PowerShell scripts should keep CRLF on Windows
*.ps1 text eol=crlf
